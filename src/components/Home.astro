---
/**
 * The homepage is deliberately short: a statement, the reasoning, the
 * holdings, the writing, one line of contact. Everything that used to sell
 * (capabilities, stats, values, FAQ, the offer) now lives on the page that
 * owns it: /tentang, /manifesto, /usaha and /kontak.
 */
import Hero from "@/components/sections/Hero.astro"
---

<Hero />
